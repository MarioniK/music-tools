# Optional dependency set for the ONNX/MusiCNN runtime target.
# Этот файл используется onnx-runtime-slim и хранится отдельно от production image layers.
# Production requirements этим артефактом не меняются.
# Default provider теперь onnx_musicnn.
# legacy_musicnn остаётся доступным для explicit rollback.
# Runtime smoke и production readiness этим файлом не разрешаются.

onnxruntime==1.25.1
essentia-tensorflow==2.1b6.dev1389
