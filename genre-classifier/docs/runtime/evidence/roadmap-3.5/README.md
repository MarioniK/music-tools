# Roadmap 3.5 Evidence

This directory stores isolated runtime parity validation evidence for `genre-classifier`.

Do not add heavyweight audio fixtures here. Use `fixture-manifest.md` to document local-only fixture paths.

Expected subdirectories:

- `baseline/` for current production runtime captures.
- `candidate-py312-etf/` for Python 3.12 + `essentia-tensorflow` candidate captures.
- `comparison/` for generated comparison reports.
